name: Keep Alive Reflecta Core

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Reflecta Core backend
        run: curl -s https://reflecta-core.onrender.com/api/ping > /dev/null